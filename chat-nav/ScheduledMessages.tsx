// import { useSelector } from 'src/redux/store';
import Scrollbar from 'src/components/scrollbar';

type Props = {};

export default function ScheduledMessages(props: Props) {
  return <Scrollbar>Scheduled Messages</Scrollbar>;
}
